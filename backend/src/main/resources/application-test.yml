spring:
  datasource:
    url: jdbc:tc:postgresql:16:///traction_test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
    target: 4
jwt:
  secret: test-secret-key-for-testing-only-must-be-256-bits-abcdefghijklmnop
  expiration: 3600000
